{
	"name": "@financica/stripe-tax-invoices",
	"version": "0.1.0",
	"description": "Parser for the Stripe tax invoice PDF — the monthly invoice for Stripe's own fees, which the Dashboard is the only place to get.",
	"keywords": [
		"accounting",
		"fees",
		"invoice",
		"pdf",
		"reverse-charge",
		"stripe",
		"tax-invoice",
		"typescript",
		"vat"
	],
	"homepage": "https://github.com/financica/ts-libs/tree/main/packages/stripe-tax-invoices#readme",
	"bugs": "https://github.com/financica/ts-libs/issues",
	"license": "MIT",
	"author": "Financica",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/financica/ts-libs.git",
		"directory": "packages/stripe-tax-invoices"
	},
	"files": [
		"dist",
		"src",
		"!src/**/*.test.ts",
		"!src/**/*.test.tsx",
		"!src/**/__tests__/**",
		"CHANGELOG.md",
		"LICENSE",
		"README.md"
	],
	"type": "module",
	"sideEffects": false,
	"main": "./dist/index.cjs",
	"module": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js",
			"require": "./dist/index.cjs"
		},
		"./package.json": "./package.json"
	},
	"publishConfig": {
		"access": "public"
	},
	"scripts": {
		"build": "tsdown",
		"clean": "rm -rf dist",
		"typecheck": "tsc --noEmit",
		"lint": "oxlint",
		"lint:fix": "oxlint --fix",
		"format": "oxfmt",
		"format:check": "oxfmt --check",
		"test": "vitest run --root ../.. --project @financica/stripe-tax-invoices",
		"test:watch": "vitest --root ../.. --project @financica/stripe-tax-invoices",
		"test:coverage": "vitest --root ../.. --project @financica/stripe-tax-invoices run --coverage",
		"ci": "bun run lint && bun run format:check && bun run build && bun run typecheck && bun run test",
		"prepack": "bun run build"
	},
	"dependencies": {
		"unpdf": "^1.8.1"
	},
	"engines": {
		"node": ">=24"
	}
}

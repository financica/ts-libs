{
	"name": "@financica/scrada-stripe",
	"version": "0.1.1",
	"description": "Convert Stripe invoices and credit notes into Scrada Peppol invoice payloads.",
	"homepage": "https://github.com/financica/scrada-stripe#readme",
	"bugs": "https://github.com/financica/scrada-stripe/issues",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/financica/scrada-stripe.git"
	},
	"files": [
		"dist"
	],
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"import": "./dist/index.js",
			"types": "./dist/index.d.ts"
		}
	},
	"scripts": {
		"build": "tsup",
		"prepack": "bun run build",
		"lint": "biome check .",
		"format": "biome format --write .",
		"type-check": "tsc --noEmit",
		"test": "vitest",
		"test:run": "vitest --run",
		"ci": "bun run type-check && bun run lint && bun run test:run && bun run build"
	},
	"dependencies": {
		"@financica/scrada-client": "^0.2.1"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.13",
		"@types/node": "^22.0.0",
		"stripe": "^22.0.2",
		"tsup": "^8.0.0",
		"typescript": "^5.9.3",
		"vitest": "^4.0.18"
	},
	"peerDependencies": {
		"stripe": ">=18.0.0"
	},
	"engines": {
		"node": ">=18"
	}
}
